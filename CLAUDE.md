# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty: it contains only an empty `index.html` file and no build tooling, dependencies, tests, or documentation. There is no package manager config, no framework, and no version control history to infer intent from.

Before adding architecture or command documentation here, confirm the project's purpose and stack with the user. Update this file once real structure exists — do not invent conventions that aren't in the code.
